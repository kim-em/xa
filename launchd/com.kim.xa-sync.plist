<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.kim.xa-sync</string>

    <!-- Pulls the snapshot to ~/.cache/xa so that `xa` never touches the
         network, and drains any acknowledgements made while disconnected.
         Install this on every machine except the one running the daemon.
         The tailscale address rather than the hostname: the daemon binds only
         to the tailnet, and plain DNS may resolve the name to a LAN address
         nothing is listening on. -->
    <key>ProgramArguments</key>
    <array>
        <string>/Users/kim/projects/xa/.venv/bin/xa-sync</string>
        <string>--url</string>
        <string>http://100.122.142.62:8787</string>
        <string>--interval</string>
        <string>30</string>
    </array>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
        <key>XA_POLICY</key>
        <string>/Users/kim/metacortex/xa</string>
    </dict>

    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/Users/kim/.local/state/xa/sync.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/kim/.local/state/xa/sync.log</string>
</dict>
</plist>
